## PCHelper — UI Polish Sprint: Global style system + Animations + Common components + Theming

Current project: 104 files, 19,111 lines, zero TS errors. 32 nav items, dark theme. 
All 36 components use inline styles + basic CSS classes. NO animation system, NO toast/notification system, NO transition framework.

### Mission: Complete UI polish — global style system, animations, common components, enhanced layout

### Task 1: Global Theme System (`src/styles/theme.css`) — Refactor & Expand

Current: 577 lines, basic CSS variables, flat styles.
Replace with a COMPREHENSIVE design system:

**New CSS Variables (add, don't remove existing):**
```css
:root {
  // Existing colors preserved - just add:
  
  /* Enhanced shadows */
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.3);
  --shadow-md: 0 4px 12px rgba(0,0,0,0.35);
  --shadow-lg: 0 8px 24px rgba(0,0,0,0.4);
  --shadow-xl: 0 12px 40px rgba(0,0,0,0.5);
  
  /* Transitions */
  --transition-fast: 0.12s ease;
  --transition-normal: 0.2s ease;
  --transition-slow: 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  
  /* Border radius variants */
  --radius-sm: 4px;
  --radius-md: 6px;  // same as --radius
  --radius-lg: 10px;
  --radius-xl: 14px;
  
  /* Z-index layers */
  --z-sidebar: 100;
  --z-titlebar: 200;
  --z-modal: 300;
  --z-toast: 400;
  --z-tooltip: 500;
}
```

**New utility classes to add:**
- `.text-xs` (10px), `.text-sm` (12px, already exists), `.text-base` (14px), `.text-lg` (16px), `.text-xl` (20px), `.text-2xl` (24px)
- `.font-mono` (already exists), `.font-medium` (500), `.font-semibold` (600), `.font-bold` (700)
- `.text-center`, `.text-right`
- `.w-full` (100%), `.h-full` (100%)
- `.overflow-auto`, `.overflow-hidden`
- `.relative`, `.absolute`
- `.cursor-pointer`
- `.select-none`
- `.whitespace-nowrap`
- `.gap-1` (4px), `.gap-2` (8px), `.gap-3` (12px), `.gap-4` (16px)
- `.p-1` (4px), `.p-2` (8px), `.p-3` (12px), `.p-4` (16px)
- `.px-2`, `.px-3`, `.px-4`
- `.py-1`, `.py-2`
- `.ml-auto`

**Animation classes:**
```css
/* Fade */
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
@keyframes fadeOut { from { opacity: 1; } to { opacity: 0; } }

/* Slide */
@keyframes slideInUp { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }
@keyframes slideInDown { from { opacity: 0; transform: translateY(-12px); } to { opacity: 1; transform: translateY(0); } }
@keyframes slideInLeft { from { opacity: 0; transform: translateX(-12px); } to { opacity: 1; transform: translateX(0); } }

/* Scale */
@keyframes scaleIn { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } }

/* Shimmer for loading */
@keyframes shimmer { 0% { background-position: -200% 0; } 100% { background-position: 200% 0; } }

/* Pulse glow */
@keyframes pulse-glow { 0%, 100% { box-shadow: 0 0 4px currentColor; } 50% { box-shadow: 0 0 12px currentColor; } }

/* Rotate (already has spin) */
@keyframes ping { 0% { transform: scale(1); opacity: 0.8; } 100% { transform: scale(2); opacity: 0; } }

.animate-fadeIn { animation: fadeIn var(--transition-normal) both; }
.animate-slideUp { animation: slideInUp var(--transition-normal) both; }
.animate-slideDown { animation: slideInDown var(--transition-normal) both; }
.animate-slideLeft { animation: slideInLeft var(--transition-normal) both; }
.animate-scaleIn { animation: scaleIn var(--transition-normal) both; }
.animate-shimmer { background: linear-gradient(90deg, var(--bg-tertiary) 25%, var(--bg-hover) 50%, var(--bg-tertiary) 75%); background-size: 200% 100%; animation: shimmer 1.5s infinite; }
.animate-ping { animation: ping 1s cubic-bezier(0, 0, 0.2, 1) infinite; }
```

**Badge system:**
```css
.badge { display: inline-flex; align-items: center; gap: 4px; padding: 2px 8px; border-radius: 10px; font-size: 10px; font-weight: 600; white-space: nowrap; }
.badge-green { background: rgba(63, 185, 80, 0.15); color: var(--green); border: 1px solid rgba(63, 185, 80, 0.3); }
.badge-yellow { background: rgba(210, 153, 34, 0.15); color: var(--yellow); border: 1px solid rgba(210, 153, 34, 0.3); }
.badge-red { background: rgba(248, 81, 73, 0.15); color: var(--red); border: 1px solid rgba(248, 81, 73, 0.3); }
.badge-blue { background: rgba(88, 166, 255, 0.15); color: var(--accent); border: 1px solid rgba(88, 166, 255, 0.3); }
.badge-gray { background: rgba(139, 148, 158, 0.15); color: var(--text-secondary); border: 1px solid rgba(139, 148, 158, 0.3); }
.badge-orange { background: rgba(219, 109, 40, 0.15); color: var(--orange); border: 1px solid rgba(219, 109, 40, 0.3); }
.badge-purple { background: rgba(163, 113, 247, 0.15); color: var(--purple); border: 1px solid rgba(163, 113, 247, 0.3); }
```

**Empty state:**
```css
.empty-state { display: flex; flex-direction: column; align-items: center; justify-content: center; padding: 48px 24px; gap: 12px; color: var(--text-muted); text-align: center; }
.empty-state-icon { opacity: 0.4; }
.empty-state-title { font-size: 16px; font-weight: 600; color: var(--text-secondary); }
.empty-state-desc { font-size: 13px; max-width: 320px; line-height: 1.5; }
```

**Loading state:**
```css
.skeleton { background: linear-gradient(90deg, var(--bg-tertiary) 25%, var(--bg-hover) 50%, var(--bg-tertiary) 75%); background-size: 200% 100%; animation: shimmer 1.5s infinite; border-radius: var(--radius-sm); }
.skeleton-text { height: 14px; width: 60%; margin-bottom: 8px; }
.skeleton-title { height: 20px; width: 40%; margin-bottom: 12px; }
.skeleton-card { height: 120px; width: 100%; }
.skeleton-row { height: 32px; width: 100%; margin-bottom: 4px; }
```

**Tooltip:**
```css
.tooltip-container { position: relative; display: inline-flex; }
.tooltip { position: absolute; bottom: calc(100% + 6px); left: 50%; transform: translateX(-50%); background: var(--bg-tertiary); border: 1px solid var(--border-color); color: var(--text-primary); padding: 4px 10px; border-radius: var(--radius-sm); font-size: 11px; white-space: nowrap; pointer-events: none; opacity: 0; transition: opacity var(--transition-fast); z-index: var(--z-tooltip); }
.tooltip-container:hover .tooltip { opacity: 1; }
```

### Task 2: Dashboard Enhancement (`src/components/Dashboard/index.tsx`)

Current: 433 lines, cards with inline styles, basic sparklines.
Enhance:
- **Page entrance animation**: `.animate-fadeIn` on mount
- **Card stagger animation**: each stat card gets `.animate-slideUp` with increasing delay via inline style: `animation-delay: 0.1s, 0.15s, 0.2s, 0.25s`
- **Value transitions**: when CPU/memory/disk/GPU values change, add a brief class that makes the number slightly pulse/glow (use a CSS highlight pulse animation)
- **Card glow on value change**: briefly set box-shadow color based on usage (green/yellow/red) then fade back
- **Mini sparklines**: add `.animate-fadeIn` to chart containers
- **Performance tabs**: add a subtle sliding indicator under active tab tab

### Task 3: Enhanced Button Variants (`src/styles/theme.css`)

Add to existing button block:
```css
.btn-danger { background: rgba(248, 81, 73, 0.1); border-color: rgba(248, 81, 73, 0.3); color: var(--red); }
.btn-danger:hover { background: var(--red); border-color: var(--red); color: #fff; }

.btn-success { background: rgba(63, 185, 80, 0.1); border-color: rgba(63, 185, 80, 0.3); color: var(--green); }
.btn-success:hover { background: var(--green); border-color: var(--green); color: #fff; }

.btn-warning { background: rgba(210, 153, 34, 0.1); border-color: rgba(210, 153, 34, 0.3); color: var(--yellow); }
.btn-warning:hover { background: var(--yellow); border-color: var(--yellow); color: #000; }

.btn-icon { padding: 6px; min-width: 32px; min-height: 32px; }  // square icon button

.btn:active { transform: scale(0.97); }  // press effect on all buttons
```

### Task 4: Loading States Across Components

Create `src/components/common/LoadingState.tsx`:
```tsx
// Reusable loading components
export function SkeletonCard() { return <div className="card skeleton-card" />; }
export function SkeletonTable({ rows = 5 }: { rows?: number }) {
  return <div className="flex-col gap-1 p-4">{Array.from({length: rows}).map((_, i) => <div key={i} className="skeleton skeleton-row" />)}</div>;
}
export function LoadingSpinner({ size = 20 }: { size?: number }) {
  return <div className="flex items-center justify-center p-8"><Loader2 size={size} className="spin" style={{color:'var(--text-muted)'}} /></div>;
}
export function EmptyState({ icon, title, desc }: { icon: React.ReactNode; title: string; desc?: string }) {
  return <div className="empty-state"><div className="empty-state-icon">{icon}</div><div className="empty-state-title">{title}</div>{desc && <div className="empty-state-desc">{desc}</div>}</div>;
}
```

Create `src/components/common/Badge.tsx`:
```tsx
type BadgeVariant = 'green' | 'yellow' | 'red' | 'blue' | 'gray' | 'orange' | 'purple';
export function Badge({ variant = 'gray', children, pulse }: { variant?: BadgeVariant; children: React.ReactNode; pulse?: boolean }) {
  return <span className={`badge badge-${variant} ${pulse ? 'animate-pulse' : ''}`}>{children}</span>;
}
```

Create `src/components/common/PageHeader.tsx`:
```tsx
interface PageHeaderProps { title: string; subtitle?: string; actions?: React.ReactNode; }
export function PageHeader({ title, subtitle, actions }: PageHeaderProps) {
  return <div className="flex items-center justify-between mb-4 animate-fadeIn">
    <div><h2 className="text-xl font-semibold">{title}</h2>{subtitle && <div className="text-sm text-muted mt-1">{subtitle}</div>}</div>
    {actions && <div className="flex gap-2">{actions}</div>}
  </div>;
}
```

Create `src/components/common/StatCard.tsx`:
```tsx
interface StatCardProps { icon: React.ReactNode; label: string; value: string; unit?: string; color?: string; trend?: 'up' | 'down' | 'neutral'; }
export function StatCard({ icon, label, value, unit, color, trend }: StatCardProps) {
  return <div className="card animate-slideUp" style={{ animationFillMode: 'both' }}>
    <div className="flex items-center gap-3">
      <div className="stat-icon" style={{ color: color || 'var(--accent)' }}>{icon}</div>
      <div><div className="stat-value">{value}<span className="stat-unit">{unit}</span></div>
      <div className="stat-label">{label}</div></div>
    </div>
  </div>;
}
```

### Task 5: Enhanced Sidebar (`src/components/Sidebar/index.tsx`)

Current improvements:
- Group nav items into sections with tiny section headers:
  - "MONITORING": Dashboard, Hardware, Software, Temps, Network
  - "SYSTEM": Processes, Startup, Services, Drivers
  - "TOOLS": Benchmark, Registry, Disk Cleanup, File Types
  - "SECURITY": Firewall, Security, USB Devices  
  - "MANAGEMENT": Apps, Updates, Scheduler, Power Plan
  - "DATA": Health, Alerts, Conflict, Battery, Clipboard, Perf Log, Event Log, Net Connections
  - "OTHER": Display, System Info, AI Chat, Settings
- Each section: uppercase label in 10px text, muted color, no click
- Nav items no longer have labels (icon only? No — keep labels, just group them)
- Actually simplest approach: just add separator lines with labels between groups
- Keep existing nav items exactly the same, just add grouping headers between them
- Hover highlight on nav items: subtle left border accent (3px accent bar)
- Active item: left border accent + slightly stronger background
- Scrolling: smooth scrolling for long sidebar

### Task 6: Table Enhancements (`src/styles/theme.css`)

Enhanced table styles:
```css
/* Sort indicator */
th.sort-asc::after { content: ' ▲'; font-size: 8px; }
th.sort-desc::after { content: ' ▼'; font-size: 8px; }

/* Row clickable */
tr.clickable { cursor: pointer; }

/* Selected row */
tr.selected td { background: var(--accent-muted); }

/* Table with alternating row color */
table.striped tr:nth-child(even) td { background: rgba(255,255,255,0.01); }

/* Compact table */
table.compact td { padding: 6px 12px; font-size: 12px; }
table.compact th { padding: 6px 12px; font-size: 10px; }
```

### Task 7: Modal/Overlay Enhancements (`src/styles/theme.css`)

```css
.modal-overlay { position: fixed; inset: 0; background: rgba(0,0,0,0.6); display: flex; align-items: center; justify-content: center; z-index: var(--z-modal); animation: fadeIn var(--transition-fast) both; }
.modal-content { background: var(--bg-secondary); border: 1px solid var(--border-color); border-radius: var(--radius-lg); padding: 24px; min-width: 400px; max-width: 600px; max-height: 80vh; overflow: auto; animation: scaleIn var(--transition-normal) both; box-shadow: var(--shadow-xl); }
.modal-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px; padding-bottom: 12px; border-bottom: 1px solid var(--border-color); }
.modal-title { font-size: 16px; font-weight: 600; }
.modal-close { background: none; border: none; color: var(--text-secondary); cursor: pointer; padding: 4px; border-radius: var(--radius-sm); }
.modal-close:hover { background: var(--bg-hover); color: var(--text-primary); }
.modal-footer { display: flex; justify-content: flex-end; gap: 8px; margin-top: 16px; padding-top: 12px; border-top: 1px solid var(--border-color); }
```

### Task 8: Implement common components usage

Replace inline Empty State + Loading patterns across ALL existing components where applicable:
- Scan `src/components/` for patterns like:
  - `{!data && <div>Loading...</div>}` → `<LoadingSpinner />`
  - `{items.length === 0 && <div>No items found</div>}` → `<EmptyState />`
- Import from `../common/...` where appropriate

### IMPORTANT:
1. After ALL changes: `npx tsc --noEmit` (zero errors required)
2. npm install already done, do NOT re-run it
3. Do NOT break any existing component functionality
4. Keep the dark theme aesthetic — this should make it look MORE professional
5. The theme.css file will get significantly larger — that's expected and good
